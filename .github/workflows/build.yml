name: Build and Release Electron App

on:
  push:
    tags:
      - 'v*'

jobs:
  # STEP 1: Create the Release anchor first so Electron Forge has a landing pad
  create-release:
    name: Initialize GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      upload_url: ${{ steps.create_gh_release.outputs.upload_url }}
    steps:
      - name: Check out git repository
        uses: actions/checkout@v4

      - name: Create GitHub Release Wrapper
        id: create_gh_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # STEP 2: Run the compilation matrix and attach files safely
  release:
    name: Build for ${{ matrix.os }}
    needs: create-release # ◄ Forces this job to wait until Step 1 completely finishes
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: Check out git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'yarn'

      - name: Force Git to use HTTPS instead of SSH
        run: git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Run Vite Production Build
        run: yarn build

      # LINUX PUBLISH
      - name: Compile and Publish via Electron Forge (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: xvfb-run --auto-servernum --server-args="-screen 0 1024x768x24" yarn run publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISABLE_ELECTRON_SANDBOX: 1
          ELECTRON_RUN_AS_NODE: 1

      # WINDOWS PUBLISH
      - name: Compile and Publish via Electron Forge (Windows)
        if: matrix.os == 'windows-latest'
        run: yarn run publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISABLE_ELECTRON_SANDBOX: 1
          ELECTRON_RUN_AS_NODE: 1
          ELECTRON_DISABLE_GPU: 1

      # MAC PUBLISH
      - name: Compile and Publish via Electron Forge (Mac)
        if: matrix.os == 'macos-latest'
        run: yarn run publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}